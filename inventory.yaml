all:
  children:
    manager:
      hosts:
        deathstar-manager:
    workers:
      hosts:
        deathstar-worker:
  hosts:
    nginx:
  vars:
    andible_user: kelvin
      ansible_ssh_private_key_file: "~/.ssh/id_rsa"
      ansible_ssh_common_args: '-o StrictHostKeyChecking=no'